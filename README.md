# R-2R-R Molecule

Small analysis workspace for normalized coupled-mode-theory (CMT) simulations of a Kerr microresonator / mode molecule.

## Requirements

Python 3 with:

- `numpy`
- `scipy`
- `matplotlib`
- `jupyter` (for the notebook)